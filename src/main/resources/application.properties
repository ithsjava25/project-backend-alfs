gg.jte.development-mode=true
spring.application.name=alfs

#Disable whitelabel
spring.web.error.whitelabel.enabled=false

# Storage (MinIO/S3) ? default local dev values; override via env in andra miljoer
storage.s3.endpoint=http://localhost:9000
storage.s3.accessKey=minio
storage.s3.secretKey=minio123
storage.s3.bucket=alfs-attachments
storage.s3.region=us-east-1
storage.s3.secure=false
# Max giltighetstid i sekunder for presignerade URL:er (kan overridas via env)
storage.s3.presign-max-ttl-seconds=86400

# Multipart upload limits (kan justeras vid behov)
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
# H2 DATABASE CONFIG
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# H2 CONSOLE
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update


# NOTE: This JWT secret is hardcoded for development/demo purposes only.
# In a production environment, it should be stored securely using
# environment variables or a secret manager.
jwt.secret=bXlTdXBlclNlY3JldEtleU15U3VwZXJTZWNyZXRLZXk=
jwt.expiration=86400000
